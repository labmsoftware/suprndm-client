{% block underlay %}

<div
    class="absolute top-0 left-0 w-full h-screen -z-50
        border border-white">

    <div
        class="flex flex-col justify-center align-middle items-center w-full h-full space-y-9">
        
        <div
            class="">
        
            <img src="/assets/dsat_logo_white.png" class="w-32">
        </div>

        <div
            class="flex flex-col justify-center align-middle items-center space-y-3">

            <h1 class="font-bold text-6xl text-white">{{ client.name }}</h1>
            <h2 class="text-2xl text-white">Waiting for content...</h2>
        </div>

        <div>
            <p class="font-mono text-stone-700">{{ client.ipAddress }}</p>
        </div>
    </div>
</div>

{% endblock %}